# Create Clip — Program Ada, GUI Tidak Ada
File: `web/components/home.js`

Tidak ada temuan spesifik di halaman ini.
Catatan: hasil clip di halaman ini dipengaruhi oleh `watermark_settings`, `credit_watermark_settings`,
`hook_style_settings`, `face_tracking_mode`, `mediapipe_settings` yang backend-nya AKTIF dipakai tapi
GUI-nya tidak ada di manapun di web UI baru — lihat `04-settings_program-belum-ada-gui.md`.
